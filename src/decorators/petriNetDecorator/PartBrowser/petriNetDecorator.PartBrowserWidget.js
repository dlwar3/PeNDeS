/*globals define, _, DEBUG, $*/
/*eslint-env browser*/

/**
 * Generated by DecoratorGenerator
 */


define([
    'js/Constants',
    'js/NodePropertyNames',
    'js/Widgets/PartBrowser/PartBrowserWidget.DecoratorBase',
    'js/Widgets/DiagramDesigner/DiagramDesignerWidget.Constants',
    'text!../DiagramDesigner/petriNetDecorator.DiagramDesignerWidget.html',
    'css!../DiagramDesigner/petriNetDecorator.DiagramDesignerWidget.css',
    'css!./petriNetDecorator.PartBrowserWidget.css'
], function (CONSTANTS,
             nodePropertyNames,
             PartBrowserWidgetDecoratorBase,
             DiagramDesignerWidgetConstants,
             petriNetDecoratorDiagramDesignerWidgetTemplate) {

    'use strict';

    var DECORATOR_ID = 'petriNetDecoratorPartBrowserWidget';

    function petriNetDecoratorPartBrowserWidget(options) {
        var opts = _.extend({}, options);

        PartBrowserWidgetDecoratorBase.apply(this, [opts]);

        this.logger.debug('petriNetDecoratorPartBrowserWidget ctor');
    }

    _.extend(petriNetDecoratorPartBrowserWidget.prototype, PartBrowserWidgetDecoratorBase.prototype);
    petriNetDecoratorPartBrowserWidget.prototype.DECORATORID = DECORATOR_ID;

    /*********************** OVERRIDE DiagramDesignerWidgetDecoratorBase MEMBERS **************************/

    petriNetDecoratorPartBrowserWidget.prototype.$DOMBase = (function () {
        var el = $(petriNetDecoratorDiagramDesignerWidgetTemplate);
        //use the same HTML template as the petriNetDecorator.DiagramDesignerWidget
        //but remove the connector DOM elements since they are not needed in the PartBrowser
        el.find('.' + DiagramDesignerWidgetConstants.CONNECTOR_CLASS).remove();
        return el;
    })();

    petriNetDecoratorPartBrowserWidget.prototype.beforeAppend = function () {
        this.$el = this.$DOMBase.clone();

        //find name placeholder
        this.skinParts.$name = this.$el.find('.name');

        this._renderContent();
    };

    petriNetDecoratorPartBrowserWidget.prototype.afterAppend = function () {
    };

    petriNetDecoratorPartBrowserWidget.prototype._renderContent = function () {
        var client = this._control._client,
            nodeObj = client.getNode(this._metaInfo[CONSTANTS.GME_ID]);

        //render GME-ID in the DOM, for debugging
        if (DEBUG) {
            this.$el.attr({'data-id': this._metaInfo[CONSTANTS.GME_ID]});
        }

        if (nodeObj) {
            this.skinParts.$name.text(nodeObj.getAttribute(nodePropertyNames.Attributes.name) || '');
        }
    };

    petriNetDecoratorPartBrowserWidget.prototype.update = function () {
        this._renderContent();
    };

    return petriNetDecoratorPartBrowserWidget;
});
